# Copia este archivo a .env y completa los valores reales
# El archivo .env NO se sube al repositorio (está en .gitignore)

# ── WiFi ────────────────────────────────────────────────────────────────────
WIFI_SSID=MI_RED_WIFI
WIFI_PASSWORD=mi_contraseña_wifi

# ── MQTT / EMQX (AWS EC2) ───────────────────────────────────────────────────
MQTT_SERVER=mqtt.tudominio.com
MQTT_SERVER_IP=1.2.3.4
MQTT_PORT=8883
MQTT_USER=carrito
MQTT_PASSWORD=contraseña_mqtt

# ── AWS S3 (OTA) ─────────────────────────────────────────────────────────────
# Se configura sólo en GitHub Secrets (AWS_ACCESS_KEY_ID, etc.)
# S3_BUCKET_NAME=carrito-firmware
